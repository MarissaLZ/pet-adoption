import React from "react"
import { Box } from "@mui/material"

const Volunteer = () => {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        minHeight: "100vh",
      }}
    >
      <div>Volunteer</div>
    </Box>
  )
}

export default Volunteer
