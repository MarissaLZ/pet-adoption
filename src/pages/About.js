import React from "react"
import { Box } from "@mui/material"

const About = () => {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        minHeight: "100vh",
      }}
    >
      <div>About</div>
    </Box>
  )
}

export default About
