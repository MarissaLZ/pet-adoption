import { styled } from "@mui/material/styles"
import { Box } from "@mui/material"

export const StyledBox = styled(Box)(({ theme }) => ({
  margin: "5rem",
  padding: "20px",
  background: "rgb(0, 128, 128, .5)",
  borderRadius: "0.5rem",
}))
